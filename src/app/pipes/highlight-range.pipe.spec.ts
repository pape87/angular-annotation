import { HighlightRangePipe } from './highlight-range.pipe';

describe('HighlightRangePipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightRangePipe();
    expect(pipe).toBeTruthy();
  });
});
